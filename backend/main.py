# This file serves as the entry point for the backend application. It:
# Imports the api.py module.
# Creates a FastAPI application instance.
# Defines configurations (e.g., model path) using config.py.
# Starts the Uvicorn server to run the FastAPI application.
